<!--
For a change to the enforcement path — the gateway, policy, credentials, or the
guest bootstraps — the project asks for evidence rather than argument. See
CONTRIBUTING.md.
-->

## What this changes

## Why

## Evidence

<!--
A test that fails without the change is the strongest form. For anything
touching enforcement, `scripts/acceptance.sh` boots real VMs precisely because
unit tests cannot establish that a compromised agent cannot get out.

Paste what you ran:

  $ swift test
  $ make -C netstack test
  $ make acceptance
-->

## Does this change what sandbox protects against?

<!--
If it narrows or widens the claim, the README's "What this does NOT protect
against" section needs updating in this same PR. Overclaiming is worse than not
shipping.
-->
